apiVersion: v1
kind: Service
metadata:
  name: arcobatrip-api
  namespace: arcobatrip
  labels:
    app.kubernetes.io/name: arcobatrip-api
    app.kubernetes.io/part-of: arcobatrox
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: arcobatrip-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      name: http
