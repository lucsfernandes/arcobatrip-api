apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arcobatrip-api-ingress
  namespace: arcobatrip
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares: arcobatrip-redirect-www@kubernetescrd,arcobatrip-redirect-to-https@kubernetescrd
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
        - arcobatrip.arcobatrox.com.br
  rules:
    - host: arcobatrip.arcobatrox.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: arcobatrip-api
                port:
                  number: 80
