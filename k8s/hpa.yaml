apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: arcobatrip-api-hpa
  namespace: arcobatrip
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: arcobatrip-api
  minReplicas: 1
  maxReplicas: 1
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
