import { UserProfileContract } from "../../../contracts/contract";

/** The updated profile (with the new `avatarUrl`), in the `/users/me` shape. */
export type UploadAvatarResponseDTO = UserProfileContract;
